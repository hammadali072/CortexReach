{
  "rules": {
    ".read":  "auth != null",
    ".write": "auth != null",

    "users": {
      "$uid": {
        ".read":  "auth.uid === $uid",
        ".write": "auth.uid === $uid"
      }
    },

    "projects": {
      ".indexOn": ["userId"],
      "$projectId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },

    "leads": {
      ".indexOn": ["projectId", "email", "userId"],
      "$leadId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },

    "campaigns": {
      ".indexOn": ["projectId", "userId"],
      "$campaignId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    },

    "email_sends": {
      ".indexOn": ["projectId", "leadId", "campaignId"],
      "$sendId": {
        ".read":  "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
